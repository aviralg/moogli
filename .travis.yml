language: python
python:
    - "2.7"
before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -y libopenscenegraph-dev
    - sudo apt-get install -y python-qt4-dev
    - sudo apt-get install -y python-sip4-dev
script:  sudo python setup.py install
cache:
    - apt
